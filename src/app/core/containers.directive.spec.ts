import { ContainersDirective } from './containers.directive';

describe('ContainersDirective', () => {
  it('should create an instance', () => {
    const directive = new ContainersDirective();
    expect(directive).toBeTruthy();
  });
});
